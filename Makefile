

build:
	bundle exec jekyll build --verbose

build-drafts:
	bundle exec jekyll build --drafts --verbose

serve:
	bundle exec jekyll serve --verbose

serve-drafts:
	bundle exec jekyll serve --drafts --verbose

cv:
	echo "cv not implemented yet"
